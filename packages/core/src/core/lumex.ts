export interface Lumex {

}

/**
 * Loads and creates a new lumex instance.
 *
 * @param _cwd The current working directory.
 * @returns The lumex instance.
 */
export function loadLumex(_cwd?: string): Lumex {
  // TODO: load config, plugins, create instance.
  return {}
}

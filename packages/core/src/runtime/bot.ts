import { Client } from 'discord.js'

interface LuminaBot {

}

/**
 * Creates the bot instance.
 */
export function createClient() {
  const _lumina: LuminaBot = {

  }

  // Create discord client
  const _client = new Client({ intents: [] })
}
